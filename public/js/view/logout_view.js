// Generated by CoffeeScript 1.3.3
(function() {
  var __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  define(['underscore', 'backbone', 'util/util', 'model/loading', 'view/loading_view', 'text!template/logout.html'], function(_, Backbone, Util, Loading, LoadingView, LogoutTemplate) {
    var LogoutView;
    LogoutView = (function(_super) {

      __extends(LogoutView, _super);

      function LogoutView() {
        return LogoutView.__super__.constructor.apply(this, arguments);
      }

      LogoutView.prototype.el = "#main";

      LogoutView.prototype.template = _.template(LogoutTemplate);

      LogoutView.prototype.initialize = function() {
        return _.bindAll(this);
      };

      LogoutView.prototype.render = function() {
        var loadingView, logoutIFrame, onloadTimes;
        Util.debug("LogoutView render");
        this.$el.html(this.template(""));
        logoutIFrame = this.$el.find("#googleLogout")[0];
        loadingView = new LoadingView;
        loadingView.render();
        onloadTimes = 0;
        logoutIFrame.onload = function() {
          Util.debug("logout done");
          onloadTimes += 1;
          if (onloadTimes > 4) {
            Util.loc.href = "" + Util.loc.protocol + "//" + Util.loc.host;
            return console.log(Util.loc.href + " href");
          }
        };
        return logoutIFrame.src = "https://accounts.google.com/Logout";
      };

      return LogoutView;

    })(Backbone.View);
    return LogoutView;
  });

}).call(this);
