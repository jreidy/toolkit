// Generated by CoffeeScript 1.3.3
(function() {
  var __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  define(['underscore', 'backbone', 'util/util', 'model/user', 'model/loading', 'model/location', 'view/navbar_view', 'view/loading_view', 'text!template/location_home.html'], function(_, Backbone, Util, User, Loading, Location, NavbarView, LoadingView, LocationHomeTemplate) {
    var LocationHomeView;
    return LocationHomeView = (function(_super) {

      __extends(LocationHomeView, _super);

      function LocationHomeView() {
        return LocationHomeView.__super__.constructor.apply(this, arguments);
      }

      LocationHomeView.prototype.el = "#main";

      LocationHomeView.prototype.template = _.template(LocationHomeTemplate);

      LocationHomeView.prototype.initialize = function() {
        var navbarView;
        _.bindAll(this);
        this.router = window.EngApp.appRouter;
        this.bindEvents();
        this._preRender();
        this.user = new User;
        this.user.on("change:userName", this.userDataReady);
        return navbarView = new NavbarView({
          model: this.user
        });
      };

      LocationHomeView.prototype.bindEvents = function() {
        var events;
        this.$el.undelegate("#office");
        events = {};
        events["change #office"] = "set";
        return this.delegateEvents(events);
      };

      LocationHomeView.prototype._preRender = function() {
        this.loadingView = new LoadingView({
          model: new Loading({
            "message": "rendering set home location page"
          })
        });
        return this.loadingView.render();
      };

      LocationHomeView.prototype.userDataReady = function() {
        this.loadingView.empty();
        this.location = new Location;
        this.location.set("id", this.user.get("email"));
        this.locationPromise = this.location.fetch();
        return this.locationPromise.done(this.render);
      };

      LocationHomeView.prototype.render = function() {
        return this.$el.html(this.template(this.location.toJSON()));
      };

      LocationHomeView.prototype.set = function(event) {
        var officeName,
          _this = this;
        officeName = $(event.target).val();
        this.location.set({
          home: officeName
        });
        return this.location.save().done(function() {
          return _this.router.navigate("beacon", {
            trigger: true
          });
        });
      };

      return LocationHomeView;

    })(Backbone.View);
  });

}).call(this);
